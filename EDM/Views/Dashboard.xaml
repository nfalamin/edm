<UserControl x:Class="EDM.Views.Dashboard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EDM.Views"
             xmlns:converters="clr-namespace:EDM.Converters"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>
        <converters:BytesToGBConverter x:Key="BytesToGBConverter"/>
        <converters:SpeedFormatter x:Key="SpeedFormatter"/>
        <converters:CountFormatter x:Key="CountFormatter"/>
    </UserControl.Resources>

    <!-- Main 4-row layout -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ==================== TOP HEADER BAR ==================== -->
        <Border x:Name="TopHeaderBar" Grid.Row="0" Background="{DynamicResource HeaderBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search Bar — Full width, larger and prominent -->
                <Border x:Name="SearchBoxBorder" Grid.Column="0" Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                        CornerRadius="10" Padding="14,0" Height="40"
                        HorizontalAlignment="Stretch" Margin="0,0,16,0">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="BorderBrush" Value="#7C3AED"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🔍" FontSize="15" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Grid.Column="1" x:Name="SearchTextBox" Background="Transparent" BorderThickness="0"
                                 Foreground="{DynamicResource PrimaryTextBrush}" Padding="0" VerticalAlignment="Center"
                                 FontSize="13" Text="" CaretBrush="{DynamicResource PrimaryTextBrush}"
                                 TextChanged="SearchTextBox_TextChanged"/>
                        <TextBlock Grid.Column="1" x:Name="SearchWatermark" Text="Search downloads..."
                                   Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" IsHitTestVisible="False"
                                   FontSize="13" Margin="2,0,0,0"/>
                    </Grid>
                </Border>

                <!-- Right Controls -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right"
                            VerticalAlignment="Center" Margin="12,0,0,0">

                    <!-- ⚡ 1-Click Live Speed Limiter Pill with Dropdown Flyout -->
                    <Grid Margin="0,0,10,0">
                        <Button x:Name="SpeedLimiterBtn" Click="SpeedLimiterBtn_Click"
                                Height="36" Background="Transparent" BorderThickness="0"
                                Cursor="Hand" ToolTip="1-Click Live Speed Limiter &amp; Bandwidth Allocation">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="SpeedLimiterBrd" Background="{DynamicResource CardInputBg}" 
                                            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" 
                                            CornerRadius="10" Padding="10,0" Height="36">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="SpeedLimiterBrd" Property="Background" Value="{DynamicResource NavHoverBg}"/>
                                            <Setter TargetName="SpeedLimiterBrd" Property="BorderBrush" Value="#00C9A7"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock x:Name="SpeedLimiterIcon" Text="⚡" FontSize="14" Foreground="#F59E0B" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock x:Name="SpeedLimiterText" Text="Speed Limit: OFF" FontSize="11.5" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                                <TextBlock Text=" ▾" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" Margin="4,0,0,0"/>
                            </StackPanel>
                        </Button>

                        <!-- Speed Limiter Preset Selection Popup -->
                        <Popup x:Name="SpeedLimiterPopup" StaysOpen="False" AllowsTransparency="True"
                               PlacementTarget="{Binding ElementName=SpeedLimiterBtn}" Placement="Bottom"
                               HorizontalOffset="-40" VerticalOffset="8">
                            <Border Width="220" Background="{DynamicResource SecondaryBackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                    CornerRadius="12" Padding="8">
                                <Border.Effect>
                                    <DropShadowEffect Color="#000000" BlurRadius="16" ShadowDepth="4" Opacity="0.4"/>
                                </Border.Effect>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="⚡ Live Speed Limiter" FontWeight="Bold" FontSize="12" 
                                               Foreground="{DynamicResource PrimaryTextBrush}" Margin="8,4,8,6"/>
                                    <Border Height="1" Background="{DynamicResource BorderBrush}" Margin="0,0,0,6"/>

                                    <Button Tag="0" Click="SelectSpeedLimitPreset_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,1" Padding="8,6" HorizontalContentAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🚀" FontSize="13" Margin="0,0,8,0"/>
                                            <TextBlock Text="Off (Unlimited Speed)" FontSize="11.5" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                        </StackPanel>
                                    </Button>

                                    <Button Tag="250" Click="SelectSpeedLimitPreset_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,1" Padding="8,6" HorizontalContentAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🐢" FontSize="13" Margin="0,0,8,0"/>
                                            <TextBlock Text="250 KB/s (Ultra Low)" FontSize="11.5" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                        </StackPanel>
                                    </Button>

                                    <Button Tag="500" Click="SelectSpeedLimitPreset_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,1" Padding="8,6" HorizontalContentAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="⚡" FontSize="13" Foreground="#F59E0B" Margin="0,0,8,0"/>
                                            <TextBlock Text="500 KB/s (Browsing Mode)" FontSize="11.5" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                        </StackPanel>
                                    </Button>

                                    <Button Tag="1024" Click="SelectSpeedLimitPreset_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,1" Padding="8,6" HorizontalContentAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="⚡" FontSize="13" Foreground="#10B981" Margin="0,0,8,0"/>
                                            <TextBlock Text="1.0 MB/s (Balanced)" FontSize="11.5" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                        </StackPanel>
                                    </Button>

                                    <Button Tag="2048" Click="SelectSpeedLimitPreset_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,1" Padding="8,6" HorizontalContentAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="⚡" FontSize="13" Foreground="#06B6D4" Margin="0,0,8,0"/>
                                            <TextBlock Text="2.0 MB/s (Standard)" FontSize="11.5" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                        </StackPanel>
                                    </Button>

                                    <Button Tag="5120" Click="SelectSpeedLimitPreset_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,1" Padding="8,6" HorizontalContentAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="⚡" FontSize="13" Foreground="#3B82F6" Margin="0,0,8,0"/>
                                            <TextBlock Text="5.0 MB/s (Fast)" FontSize="11.5" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                        </StackPanel>
                                    </Button>

                                    <Button Tag="10240" Click="SelectSpeedLimitPreset_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,1" Padding="8,6" HorizontalContentAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="⚡" FontSize="13" Foreground="#8B5CF6" Margin="0,0,8,0"/>
                                            <TextBlock Text="10.0 MB/s (High Speed)" FontSize="11.5" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Border>
                        </Popup>
                    </Grid>

                    <!-- 📥 Always-On-Top Desktop Drop Basket Toggle Pill -->
                    <Button x:Name="DropBasketBtn" Click="DropBasketBtn_Click"
                            Height="36" Background="Transparent" BorderThickness="0"
                            Cursor="Hand" ToolTip="Toggle Always-On-Top Desktop Drop Basket (Drag &amp; Drop links or files from any browser)"
                            Margin="0,0,10,0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="BasketPillBorder" Background="{DynamicResource CardInputBg}" 
                                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" 
                                        CornerRadius="10" Padding="10,0" Height="36">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="BasketPillBorder" Property="Background" Value="{DynamicResource NavHoverBg}"/>
                                        <Setter TargetName="BasketPillBorder" Property="BorderBrush" Value="#A855F7"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="BasketIcon" Text="📥" FontSize="14" Foreground="#A855F7" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock x:Name="BasketLabel" Text="Drop Basket: OFF" FontSize="11.5" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <!-- AI Assistant Button (🤖) -->
                    <Button x:Name="TopAiBtn" Click="TopAiBtn_Click"
                            Width="36" Height="36" Background="Transparent" BorderThickness="0"
                            Cursor="Hand" ToolTip="EDM AI Assistant (Offline Chat &amp; Diagnostics)" Margin="0,0,6,0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="AiBrd" Background="{DynamicResource CardInputBg}" 
                                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" 
                                        Width="36" Height="36" CornerRadius="10">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#8B4DFF" BlurRadius="8" ShadowDepth="1" Opacity="0.4"/>
                                    </Border.Effect>
                                    <TextBlock Text="🤖" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="AiBrd" Property="Background" Value="{DynamicResource NavHoverBg}"/>
                                        <Setter TargetName="AiBrd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <!-- 🔒 Site Logins / Auth Manager Button -->
                    <Button x:Name="TopLoginBtn" Click="TopLoginBtn_Click"
                            Width="36" Height="36" Background="Transparent" BorderThickness="0"
                            Cursor="Hand" ToolTip="Site Logins &amp; HTTP Authentication Manager" Margin="0,0,6,0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="LoginBrd" Background="{DynamicResource CardInputBg}" 
                                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" 
                                        Width="36" Height="36" CornerRadius="10">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#00C9A7" BlurRadius="8" ShadowDepth="1" Opacity="0.35"/>
                                    </Border.Effect>
                                    <TextBlock Text="🔒" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="LoginBrd" Property="Background" Value="{DynamicResource NavHoverBg}"/>
                                        <Setter TargetName="LoginBrd" Property="BorderBrush" Value="#00C9A7"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <!-- Language Selector Button (🌐) with Flyout -->
                    <Grid Margin="0,0,6,0">
                        <Button x:Name="LanguageBtn" Click="LanguageBtn_Click"
                                Width="36" Height="36" Background="Transparent" BorderThickness="0"
                                Cursor="Hand" ToolTip="Language">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="LangBrd" Background="{DynamicResource CardInputBg}" 
                                            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" 
                                            Width="36" Height="36" CornerRadius="10">
                                        <Border.Effect>
                                            <DropShadowEffect Color="#000000" BlurRadius="6" ShadowDepth="1" Opacity="0.3"/>
                                        </Border.Effect>
                                        <TextBlock Text="🌐" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="LangBrd" Property="Background" Value="{DynamicResource NavHoverBg}"/>
                                            <Setter TargetName="LangBrd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Language Selection Popup -->
                        <Popup x:Name="LanguagePopup" StaysOpen="False" AllowsTransparency="True"
                               PlacementTarget="{Binding ElementName=LanguageBtn}" Placement="Bottom"
                               HorizontalOffset="-190" VerticalOffset="8">
                            <Border Width="260" Background="{DynamicResource SecondaryBackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                    CornerRadius="14" Padding="10">
                                <Border.Effect>
                                    <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="6" Opacity="0.40"/>
                                </Border.Effect>
                                <StackPanel Orientation="Vertical">
                                    <!-- Header -->
                                    <StackPanel Orientation="Horizontal" Margin="8,6,8,10">
                                        <TextBlock Text="🌐" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="Select Language" FontWeight="Bold" FontSize="12"
                                                   Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <!-- Auto Detect Country — special highlighted row -->
                                    <Button x:Name="AutoDetectLangBtn" Click="AutoDetectLanguage_Click"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                                            Margin="0,0,0,4" Padding="0" HorizontalContentAlignment="Stretch"
                                            ToolTip="Uses internet to detect your country. No data stored.">
                                        <Border CornerRadius="10" Padding="10,8" Margin="0">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#1E1B4B" Offset="0"/>
                                                    <GradientStop Color="#312E81" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                            <Border.Effect>
                                                <DropShadowEffect Color="#7C3AED" BlurRadius="8" ShadowDepth="0" Opacity="0.4"/>
                                            </Border.Effect>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" x:Name="AutoDetectIcon" Text="🌍"
                                                           FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock Text="Auto Detect Country" FontSize="12"
                                                               FontWeight="Bold" Foreground="White"/>
                                                    <TextBlock x:Name="AutoDetectSubLabel"
                                                               Text="Detect language from your IP"
                                                               FontSize="10" Foreground="#A5B4FC"/>
                                                </StackPanel>
                                                <Border Grid.Column="2" Background="#7C3AED" CornerRadius="6"
                                                        Padding="6,3" VerticalAlignment="Center">
                                                    <TextBlock Text="NEW" FontSize="9" FontWeight="Bold"
                                                               Foreground="White"/>
                                                </Border>
                                            </Grid>
                                        </Border>
                                    </Button>

                                    <!-- Divider -->
                                    <Border Height="1" Background="{DynamicResource BorderBrush}" Margin="4,2,4,6" Opacity="0.5"/>

                                    <!-- Built-in language list -->
                                    <Button x:Name="LangBtn_enUS" Tag="en-US" Click="SelectLanguage_Click"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                                            Margin="0,1" Padding="8,7" HorizontalContentAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🇺🇸" FontSize="15" Margin="0,0,10,0"/>
                                            <TextBlock Text="English (US)" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>

                                    <Button x:Name="LangBtn_bnBD" Tag="bn-BD" Click="SelectLanguage_Click"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                                            Margin="0,1" Padding="8,7" HorizontalContentAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🇧🇩" FontSize="15" Margin="0,0,10,0"/>
                                            <TextBlock Text="বাংলা (Bengali)" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>

                                    <Button x:Name="LangBtn_hiIN" Tag="hi-IN" Click="SelectLanguage_Click"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                                            Margin="0,1" Padding="8,7" HorizontalContentAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🇮🇳" FontSize="15" Margin="0,0,10,0"/>
                                            <TextBlock Text="हिन्दी (Hindi)" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>

                                    <Button x:Name="LangBtn_esES" Tag="es-ES" Click="SelectLanguage_Click"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                                            Margin="0,1" Padding="8,7" HorizontalContentAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🇪🇸" FontSize="15" Margin="0,0,10,0"/>
                                            <TextBlock Text="Español (Spanish)" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>

                                    <Button x:Name="LangBtn_arSA" Tag="ar-SA" Click="SelectLanguage_Click"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                                            Margin="0,1" Padding="8,7" HorizontalContentAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🇸🇦" FontSize="15" Margin="0,0,10,0"/>
                                            <TextBlock Text="العربية (Arabic) [RTL]" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>

                                    <Button x:Name="LangBtn_urPK" Tag="ur-PK" Click="SelectLanguage_Click"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                                            Margin="0,1" Padding="8,7" HorizontalContentAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🇵🇰" FontSize="15" Margin="0,0,10,0"/>
                                            <TextBlock Text="اردو (Urdu) [RTL]" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>

                                    <Button x:Name="LangBtn_frFR" Tag="fr-FR" Click="SelectLanguage_Click"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                                            Margin="0,1" Padding="8,7" HorizontalContentAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🇫🇷" FontSize="15" Margin="0,0,10,0"/>
                                            <TextBlock Text="Français (French)" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>

                                    <Button x:Name="LangBtn_deDE" Tag="de-DE" Click="SelectLanguage_Click"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                                            Margin="0,1" Padding="8,7" HorizontalContentAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🇩🇪" FontSize="15" Margin="0,0,10,0"/>
                                            <TextBlock Text="Deutsch (German)" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>

                                    <Button x:Name="LangBtn_jaJP" Tag="ja-JP" Click="SelectLanguage_Click"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                                            Margin="0,1" Padding="8,7" HorizontalContentAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🇯🇵" FontSize="15" Margin="0,0,10,0"/>
                                            <TextBlock Text="日本語 (Japanese)" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>

                                    <Button x:Name="LangBtn_zhCN" Tag="zh-CN" Click="SelectLanguage_Click"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                                            Margin="0,1" Padding="8,7" HorizontalContentAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🇨🇳" FontSize="15" Margin="0,0,10,0"/>
                                            <TextBlock Text="简体中文 (Chinese)" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>

                                </StackPanel>
                            </Border>
                        </Popup>
                    </Grid>


                    <!-- Help / Support Button (❓) -->
                    <Button x:Name="TopSupportBtn" Click="TopSupportBtn_Click"
                            Width="36" Height="36" Background="Transparent" BorderThickness="0"
                            Cursor="Hand" ToolTip="Support &amp; Help Center" Margin="0,0,6,0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="SupportBrd" Background="{DynamicResource CardInputBg}" 
                                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" 
                                        Width="36" Height="36" CornerRadius="10">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#000000" BlurRadius="6" ShadowDepth="1" Opacity="0.3"/>
                                    </Border.Effect>
                                    <TextBlock Text="❓" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="SupportBrd" Property="Background" Value="{DynamicResource NavHoverBg}"/>
                                        <Setter TargetName="SupportBrd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <!-- About EDM Button (ℹ️) -->
                    <Button x:Name="TopAboutBtn" Click="TopAboutBtn_Click"
                            Width="36" Height="36" Background="Transparent" BorderThickness="0"
                            Cursor="Hand" ToolTip="About EDM &amp; Version History" Margin="0,0,6,0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="AboutBrd" Background="{DynamicResource CardInputBg}" 
                                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" 
                                        Width="36" Height="36" CornerRadius="10">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#000000" BlurRadius="6" ShadowDepth="1" Opacity="0.3"/>
                                    </Border.Effect>
                                    <TextBlock Text="ℹ️" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="AboutBrd" Property="Background" Value="{DynamicResource NavHoverBg}"/>
                                        <Setter TargetName="AboutBrd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <!-- Theme Toggle Button (Light/Dark Switch) -->
                    <Button x:Name="ThemeToggleBtn" Click="ThemeToggle_Click"
                            Width="36" Height="36" Background="Transparent" BorderThickness="0"
                            Cursor="Hand" ToolTip="Toggle Theme" Margin="0,0,6,0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="ThemeBrd" Background="{DynamicResource CardInputBg}" 
                                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" 
                                        Width="36" Height="36" CornerRadius="10">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#000000" BlurRadius="6" ShadowDepth="1" Opacity="0.3"/>
                                    </Border.Effect>
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ThemeBrd" Property="Background" Value="{DynamicResource NavHoverBg}"/>
                                        <Setter TargetName="ThemeBrd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <TextBlock x:Name="ThemeIconText" Text="🌙" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>

                    <!-- Notification Bell with Flyout Popup -->
                    <Grid Margin="0,0,10,0">
                        <Button x:Name="NotificationBtn" Click="Notification_Click"
                                Width="36" Height="36" Background="Transparent" BorderThickness="0"
                                Cursor="Hand" ToolTip="Notifications">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="NotifBrd" Background="{DynamicResource CardInputBg}" 
                                            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" 
                                            Width="36" Height="36" CornerRadius="10">
                                        <Border.Effect>
                                            <DropShadowEffect Color="#000000" BlurRadius="6" ShadowDepth="1" Opacity="0.3"/>
                                        </Border.Effect>
                                        <TextBlock Text="🔔" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="NotifBrd" Property="Background" Value="{DynamicResource NavHoverBg}"/>
                                            <Setter TargetName="NotifBrd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Notification Badge Overlay -->
                        <Grid x:Name="NotificationBadgeGrid" Visibility="Collapsed" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,2,2,0" IsHitTestVisible="False">
                            <Ellipse Width="14" Height="14" Fill="#EF4444"/>
                            <TextBlock x:Name="NotificationCountText" Text="0" FontSize="8" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>

                        <!-- Notification Flyout Popup -->
                        <Popup x:Name="NotificationPopup" StaysOpen="False" AllowsTransparency="True"
                               PlacementTarget="{Binding ElementName=NotificationBtn}" Placement="Bottom"
                               HorizontalOffset="-260" VerticalOffset="8">
                            <Border Width="320" MaxHeight="400" Background="{DynamicResource SecondaryBackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                    CornerRadius="12" Padding="14">
                                <Border.Effect>
                                    <DropShadowEffect Color="#000000" BlurRadius="16" ShadowDepth="4" Opacity="0.3"/>
                                </Border.Effect>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Popup Header -->
                                    <Grid Grid.Row="0" Margin="0,0,0,10">
                                        <TextBlock Text="Notifications" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button Content="Mark all read" FontSize="11" Foreground="{DynamicResource AccentBrush}"
                                                    Background="Transparent" BorderThickness="0" Cursor="Hand" Click="MarkAllRead_Click" Margin="0,0,8,0"/>
                                            <Button Content="Clear" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}"
                                                    Background="Transparent" BorderThickness="0" Cursor="Hand" Click="ClearNotifications_Click"/>
                                        </StackPanel>
                                    </Grid>

                                    <!-- Notifications List -->
                                    <ScrollViewer Grid.Row="1" MaxHeight="280" VerticalScrollBarVisibility="Auto">
                                        <StackPanel x:Name="NotificationListPanel" Orientation="Vertical">
                                            <TextBlock x:Name="EmptyNotificationText" Text="No notifications yet." Foreground="{DynamicResource SecondaryTextBrush}"
                                                       FontSize="12" HorizontalAlignment="Center" Margin="0,20"/>
                                        </StackPanel>
                                    </ScrollViewer>
                                </Grid>
                            </Border>
                        </Popup>
                    </Grid>

                    <!-- Profile Pill -->
                    <Border x:Name="ProfilePillBorder" Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                            CornerRadius="20" Padding="8,5" Cursor="Hand" ToolTip="User Account &amp; License"
                            MouseLeftButtonUp="ProfilePill_Click">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <!-- Avatar -->
                            <Grid Grid.Column="0" Width="28" Height="28" Margin="0,0,8,0">
                                <Ellipse>
                                    <Ellipse.Fill>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#7C3AED" Offset="0"/>
                                            <GradientStop Color="#3B82F6" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Ellipse.Fill>
                                </Ellipse>
                                <TextBlock x:Name="UserAvatarInitials" Text="EDM" FontSize="10" FontWeight="Bold" Foreground="White"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                            <!-- Name -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock x:Name="UserNameTextBlock" Text="EDM Pro User" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBlock Text="Licensed" FontSize="9" Foreground="#10B981" FontWeight="SemiBold"/>
                            </StackPanel>
                            <!-- Arrow -->
                            <TextBlock Grid.Column="2" Text="▾" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"
                                       VerticalAlignment="Center" Margin="8,0,2,0"/>
                        </Grid>
                    </Border>

                </StackPanel>
            </Grid>
        </Border>

        <!-- ==================== QUICK ACTION TOOLBAR ==================== -->
        <Border Grid.Row="1" Background="{DynamicResource HeaderBackgroundBrush}" Padding="20,12" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
            <UniformGrid Columns="8" HorizontalAlignment="Stretch">

                <!-- Add URL -->
                <Button Tag="AddUrl" Click="ActionButton_Click" Cursor="Hand" Background="Transparent" BorderThickness="0" Margin="4,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="CardBrd" Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="8">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource NavHoverBg}"/>
                                                <Setter Property="BorderBrush" Value="#8B5CF6"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <Grid Width="44" Height="44" Margin="0,0,0,7" HorizontalAlignment="Center">
                                        <Ellipse>
                                            <Ellipse.Fill>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#6366F1" Offset="0"/>
                                                    <GradientStop Color="#8B5CF6" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Ellipse.Fill>
                                            <Ellipse.Effect>
                                                <DropShadowEffect Color="#8B5CF6" BlurRadius="18" ShadowDepth="2" Opacity="0.75"/>
                                            </Ellipse.Effect>
                                        </Ellipse>
                                        <TextBlock Text="🔗" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Grid>
                                    <TextBlock Text="Add URL" FontSize="11" Foreground="{DynamicResource PrimaryTextBrush}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Resume -->
                <Button Tag="Resume" Click="ActionButton_Click" Cursor="Hand" Background="Transparent" BorderThickness="0" Margin="4,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="8">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource NavHoverBg}"/>
                                                <Setter Property="BorderBrush" Value="#10B981"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <Grid Width="44" Height="44" Margin="0,0,0,7" HorizontalAlignment="Center">
                                        <Ellipse>
                                            <Ellipse.Fill>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#10B981" Offset="0"/>
                                                    <GradientStop Color="#22C55E" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Ellipse.Fill>
                                            <Ellipse.Effect>
                                                <DropShadowEffect Color="#10B981" BlurRadius="18" ShadowDepth="2" Opacity="0.75"/>
                                            </Ellipse.Effect>
                                        </Ellipse>
                                        <TextBlock Text="▶" FontSize="16" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Grid>
                                    <TextBlock Text="Resume" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Pause -->
                <Button Tag="Pause" Click="ActionButton_Click" Cursor="Hand" Background="Transparent" BorderThickness="0" Margin="4,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="8">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource NavHoverBg}"/>
                                                <Setter Property="BorderBrush" Value="#F59E0B"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <Grid Width="44" Height="44" Margin="0,0,0,7" HorizontalAlignment="Center">
                                        <Ellipse>
                                            <Ellipse.Fill>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#F59E0B" Offset="0"/>
                                                    <GradientStop Color="#F97316" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Ellipse.Fill>
                                            <Ellipse.Effect>
                                                <DropShadowEffect Color="#F59E0B" BlurRadius="18" ShadowDepth="2" Opacity="0.75"/>
                                            </Ellipse.Effect>
                                        </Ellipse>
                                        <TextBlock Text="⏸" FontSize="16" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Grid>
                                    <TextBlock Text="Pause" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Stop -->
                <Button Tag="Stop" Click="ActionButton_Click" Cursor="Hand" Background="Transparent" BorderThickness="0" Margin="4,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="8">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource NavHoverBg}"/>
                                                <Setter Property="BorderBrush" Value="#EF4444"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <Grid Width="44" Height="44" Margin="0,0,0,7" HorizontalAlignment="Center">
                                        <Ellipse>
                                            <Ellipse.Fill>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#EF4444" Offset="0"/>
                                                    <GradientStop Color="#DC2626" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Ellipse.Fill>
                                            <Ellipse.Effect>
                                                <DropShadowEffect Color="#EF4444" BlurRadius="18" ShadowDepth="2" Opacity="0.75"/>
                                            </Ellipse.Effect>
                                        </Ellipse>
                                        <TextBlock Text="⏹" FontSize="16" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Grid>
                                    <TextBlock Text="Stop" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Delete -->
                <Button Tag="Delete" Click="ActionButton_Click" Cursor="Hand" Background="Transparent" BorderThickness="0" Margin="4,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="8">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource NavHoverBg}"/>
                                                <Setter Property="BorderBrush" Value="#EC4899"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <Grid Width="44" Height="44" Margin="0,0,0,7" HorizontalAlignment="Center">
                                        <Ellipse>
                                            <Ellipse.Fill>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#EC4899" Offset="0"/>
                                                    <GradientStop Color="#D946EF" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Ellipse.Fill>
                                            <Ellipse.Effect>
                                                <DropShadowEffect Color="#EC4899" BlurRadius="18" ShadowDepth="2" Opacity="0.75"/>
                                            </Ellipse.Effect>
                                        </Ellipse>
                                        <TextBlock Text="🗑" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Grid>
                                    <TextBlock Text="Delete" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Delete All -->
                <Button Tag="DeleteAll" Click="ActionButton_Click" Cursor="Hand" Background="Transparent" BorderThickness="0" Margin="4,0" ToolTip="Clear and Delete All Downloads">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="8">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource NavHoverBg}"/>
                                                <Setter Property="BorderBrush" Value="#EF4444"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <Grid Width="44" Height="44" Margin="0,0,0,7" HorizontalAlignment="Center">
                                        <Ellipse>
                                            <Ellipse.Fill>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#DC2626" Offset="0"/>
                                                    <GradientStop Color="#991B1B" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Ellipse.Fill>
                                            <Ellipse.Effect>
                                                <DropShadowEffect Color="#DC2626" BlurRadius="18" ShadowDepth="2" Opacity="0.75"/>
                                            </Ellipse.Effect>
                                        </Ellipse>
                                        <TextBlock Text="🧹" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Grid>
                                    <TextBlock Text="Delete All" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Scheduler -->
                <Button Tag="Scheduler" Click="ActionButton_Click" Cursor="Hand" Background="Transparent" BorderThickness="0" Margin="4,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="8">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource NavHoverBg}"/>
                                                <Setter Property="BorderBrush" Value="#06B6D4"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <Grid Width="44" Height="44" Margin="0,0,0,7" HorizontalAlignment="Center">
                                        <Ellipse>
                                            <Ellipse.Fill>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#06B6D4" Offset="0"/>
                                                    <GradientStop Color="#3B82F6" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Ellipse.Fill>
                                            <Ellipse.Effect>
                                                <DropShadowEffect Color="#06B6D4" BlurRadius="18" ShadowDepth="2" Opacity="0.75"/>
                                            </Ellipse.Effect>
                                        </Ellipse>
                                        <TextBlock Text="📅" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Grid>
                                    <TextBlock Text="Scheduler" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Site Grabber -->
                <Button Tag="Grabber" Click="ActionButton_Click" Cursor="Hand" Background="Transparent" BorderThickness="0" Margin="4,0" ToolTip="Crawl websites and batch extract all media files">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="8">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource NavHoverBg}"/>
                                                <Setter Property="BorderBrush" Value="#A855F7"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <Grid Width="44" Height="44" Margin="0,0,0,7" HorizontalAlignment="Center">
                                        <Ellipse>
                                            <Ellipse.Fill>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#A855F7" Offset="0"/>
                                                    <GradientStop Color="#7C3AED" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Ellipse.Fill>
                                            <Ellipse.Effect>
                                                <DropShadowEffect Color="#A855F7" BlurRadius="18" ShadowDepth="2" Opacity="0.75"/>
                                            </Ellipse.Effect>
                                        </Ellipse>
                                        <TextBlock Text="🕷️" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Grid>
                                    <TextBlock Text="Grabber" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

            </UniformGrid>
        </Border>

        <!-- ==================== MAIN SCROLLABLE CONTENT ==================== -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Background="{DynamicResource BackgroundBrush}">
            <StackPanel Margin="20,16,20,16">

                <!-- Download Overview Title -->
                <TextBlock Text="Download Overview" FontSize="16" FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,14"/>

                <!-- ====== 4 KPI STAT CARDS ====== -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Total Downloads Card — Click to show All -->
                    <Border Grid.Column="0" Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="18,16"
                            Cursor="Hand" MouseLeftButtonUp="OverviewCard_Click" Tag="AllDownloads">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="RenderTransform"><Setter.Value><TranslateTransform Y="0"/></Setter.Value></Setter>
                                <Setter Property="Effect"><Setter.Value><DropShadowEffect BlurRadius="12" ShadowDepth="3" Opacity="0.2" Color="#000000"/></Setter.Value></Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="BorderBrush" Value="#8B5CF6"/>
                                        <Setter Property="Effect"><Setter.Value><DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.5" Color="#8B5CF6"/></Setter.Value></Setter>
                                    </Trigger>
                                    <EventTrigger RoutedEvent="MouseEnter">
                                        <BeginStoryboard><Storyboard><DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(TranslateTransform.Y)" To="-4" Duration="0:0:0.18" DecelerationRatio="0.8"/></Storyboard></BeginStoryboard>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="MouseLeave">
                                        <BeginStoryboard><Storyboard><DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.18" AccelerationRatio="0.8"/></Storyboard></BeginStoryboard>
                                    </EventTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="Total Downloads" FontSize="11" Foreground="{DynamicResource SidebarTextBrush}" Margin="0,0,0,6"/>
                                <TextBlock Text="{Binding TotalDownloadsCount, FallbackValue=0}" FontSize="34" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBlock Text="All time" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,0"/>
                            </StackPanel>
                            <Grid Grid.Column="1" Width="52" Height="52" VerticalAlignment="Center">
                                <Ellipse>
                                    <Ellipse.Fill>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#7C3AED" Offset="0"/>
                                            <GradientStop Color="#A855F7" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Ellipse.Fill>
                                    <Ellipse.Effect>
                                        <DropShadowEffect Color="#8B5CF6" BlurRadius="18" ShadowDepth="2" Opacity="0.7"/>
                                    </Ellipse.Effect>
                                </Ellipse>
                                <TextBlock Text="☁" FontSize="24" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Grid>
                    </Border>

                    <!-- Downloading Card — Click to filter Downloading -->
                    <Border Grid.Column="2" Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="18,16"
                            Cursor="Hand" MouseLeftButtonUp="OverviewCard_Click" Tag="Downloading">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="RenderTransform"><Setter.Value><TranslateTransform Y="0"/></Setter.Value></Setter>
                                <Setter Property="Effect"><Setter.Value><DropShadowEffect BlurRadius="12" ShadowDepth="3" Opacity="0.2" Color="#000000"/></Setter.Value></Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="BorderBrush" Value="#10B981"/>
                                        <Setter Property="Effect"><Setter.Value><DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.5" Color="#10B981"/></Setter.Value></Setter>
                                    </Trigger>
                                    <EventTrigger RoutedEvent="MouseEnter">
                                        <BeginStoryboard><Storyboard><DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(TranslateTransform.Y)" To="-4" Duration="0:0:0.18" DecelerationRatio="0.8"/></Storyboard></BeginStoryboard>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="MouseLeave">
                                        <BeginStoryboard><Storyboard><DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.18" AccelerationRatio="0.8"/></Storyboard></BeginStoryboard>
                                    </EventTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="Downloading" FontSize="11" Foreground="{DynamicResource SidebarTextBrush}" Margin="0,0,0,6"/>
                                <TextBlock Text="{Binding ActiveDownloadsCount, FallbackValue=0}" FontSize="34" FontWeight="Bold" Foreground="#10B981"/>
                                <TextBlock Text="Active now" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,0"/>
                            </StackPanel>
                            <Grid Grid.Column="1" Width="52" Height="52" VerticalAlignment="Center">
                                <Ellipse>
                                    <Ellipse.Fill>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#10B981" Offset="0"/>
                                            <GradientStop Color="#22C55E" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Ellipse.Fill>
                                    <Ellipse.Effect>
                                        <DropShadowEffect Color="#10B981" BlurRadius="18" ShadowDepth="2" Opacity="0.7"/>
                                    </Ellipse.Effect>
                                </Ellipse>
                                <TextBlock Text="⬇" FontSize="22" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Grid>
                    </Border>

                    <!-- Completed Card — Click to filter Completed -->
                    <Border Grid.Column="4" Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="18,16"
                            Cursor="Hand" MouseLeftButtonUp="OverviewCard_Click" Tag="Completed">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="RenderTransform"><Setter.Value><TranslateTransform Y="0"/></Setter.Value></Setter>
                                <Setter Property="Effect"><Setter.Value><DropShadowEffect BlurRadius="12" ShadowDepth="3" Opacity="0.2" Color="#000000"/></Setter.Value></Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="BorderBrush" Value="#0EA5E9"/>
                                        <Setter Property="Effect"><Setter.Value><DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.5" Color="#0EA5E9"/></Setter.Value></Setter>
                                    </Trigger>
                                    <EventTrigger RoutedEvent="MouseEnter">
                                        <BeginStoryboard><Storyboard><DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(TranslateTransform.Y)" To="-4" Duration="0:0:0.18" DecelerationRatio="0.8"/></Storyboard></BeginStoryboard>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="MouseLeave">
                                        <BeginStoryboard><Storyboard><DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.18" AccelerationRatio="0.8"/></Storyboard></BeginStoryboard>
                                    </EventTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="Completed" FontSize="11" Foreground="{DynamicResource SidebarTextBrush}" Margin="0,0,0,6"/>
                                <TextBlock Text="{Binding CompletedDownloadsCount, FallbackValue=0}" FontSize="34" FontWeight="Bold" Foreground="#3B82F6"/>
                                <TextBlock Text="All completed" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,0"/>
                            </StackPanel>
                            <Grid Grid.Column="1" Width="52" Height="52" VerticalAlignment="Center">
                                <Ellipse>
                                    <Ellipse.Fill>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#0EA5E9" Offset="0"/>
                                            <GradientStop Color="#3B82F6" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Ellipse.Fill>
                                    <Ellipse.Effect>
                                        <DropShadowEffect Color="#0EA5E9" BlurRadius="18" ShadowDepth="2" Opacity="0.7"/>
                                    </Ellipse.Effect>
                                </Ellipse>
                                <TextBlock Text="✓" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Grid>
                    </Border>

                    <!-- Total Size Card — Click to show All -->
                    <Border Grid.Column="6" Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="18,16"
                            Cursor="Hand" MouseLeftButtonUp="OverviewCard_Click" Tag="AllDownloads">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="RenderTransform"><Setter.Value><TranslateTransform Y="0"/></Setter.Value></Setter>
                                <Setter Property="Effect"><Setter.Value><DropShadowEffect BlurRadius="12" ShadowDepth="3" Opacity="0.2" Color="#000000"/></Setter.Value></Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="BorderBrush" Value="#8B5CF6"/>
                                        <Setter Property="Effect"><Setter.Value><DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.5" Color="#8B5CF6"/></Setter.Value></Setter>
                                    </Trigger>
                                    <EventTrigger RoutedEvent="MouseEnter">
                                        <BeginStoryboard><Storyboard><DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(TranslateTransform.Y)" To="-4" Duration="0:0:0.18" DecelerationRatio="0.8"/></Storyboard></BeginStoryboard>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="MouseLeave">
                                        <BeginStoryboard><Storyboard><DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.18" AccelerationRatio="0.8"/></Storyboard></BeginStoryboard>
                                    </EventTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="Total Size" FontSize="11" Foreground="{DynamicResource SidebarTextBrush}" Margin="0,0,0,6"/>
                                <TextBlock Text="{Binding TotalSizeDownloaded, FallbackValue='0 B'}" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBlock Text="Downloaded" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,0"/>
                            </StackPanel>
                            <Grid Grid.Column="1" Width="52" Height="52" VerticalAlignment="Center">
                                <Ellipse>
                                    <Ellipse.Fill>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#8B5CF6" Offset="0"/>
                                            <GradientStop Color="#C084FC" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Ellipse.Fill>
                                    <Ellipse.Effect>
                                        <DropShadowEffect Color="#8B5CF6" BlurRadius="18" ShadowDepth="2" Opacity="0.7"/>
                                    </Ellipse.Effect>
                                </Ellipse>
                                <TextBlock Text="📁" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Grid>
                    </Border>

                </Grid>

                <!-- Downloads Table Component -->
                <local:DownloadsTable x:Name="DownloadsTableControl"/>

            </StackPanel>
        </ScrollViewer>

        <!-- ==================== BOTTOM SYSTEM STATUS BAR ==================== -->
        <Border Grid.Row="3" Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- High Speed Connection -->
                <Border Grid.Column="0" Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="12,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" Width="36" Height="36" Margin="0,0,10,0">
                            <Ellipse Fill="#10B981">
                                <Ellipse.Effect>
                                    <DropShadowEffect Color="#10B981" BlurRadius="12" ShadowDepth="1" Opacity="0.6"/>
                                </Ellipse.Effect>
                            </Ellipse>
                            <TextBlock Text="📶" FontSize="15" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding ConnectionStatus, FallbackValue='High Speed Connection'}" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                            <TextBlock Text="{Binding BandwidthStatus, FallbackValue='Unlimited Bandwidth'}" FontSize="11" FontWeight="Bold" Foreground="#10B981"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Download Speed -->
                <Border Grid.Column="2" Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="12,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" Width="36" Height="36" Margin="0,0,10,0">
                            <Ellipse Fill="#8B5CF6">
                                <Ellipse.Effect>
                                    <DropShadowEffect Color="#8B5CF6" BlurRadius="12" ShadowDepth="1" Opacity="0.6"/>
                                </Ellipse.Effect>
                            </Ellipse>
                            <TextBlock Text="⏱" FontSize="15" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Download Speed" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                            <TextBlock Text="{Binding CurrentDownloadSpeed, FallbackValue='6.2 MB/s'}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Uploaded -->
                <Border Grid.Column="4" Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="12,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" Width="36" Height="36" Margin="0,0,10,0">
                            <Ellipse Fill="#3B82F6">
                                <Ellipse.Effect>
                                    <DropShadowEffect Color="#3B82F6" BlurRadius="12" ShadowDepth="1" Opacity="0.6"/>
                                </Ellipse.Effect>
                            </Ellipse>
                            <TextBlock Text="⬆" FontSize="15" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Uploaded" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                            <TextBlock Text="{Binding TotalUploadedData, FallbackValue='128.4 MB'}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Active Downloads -->
                <Border Grid.Column="6" Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="12,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" Width="36" Height="36" Margin="0,0,10,0">
                            <Ellipse Fill="#F59E0B">
                                <Ellipse.Effect>
                                    <DropShadowEffect Color="#F59E0B" BlurRadius="12" ShadowDepth="1" Opacity="0.6"/>
                                </Ellipse.Effect>
                            </Ellipse>
                            <TextBlock Text="📊" FontSize="15" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Active Downloads" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                            <TextBlock Text="{Binding ActiveDownloadsCount, FallbackValue=6}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Max Speed -->
                <Border Grid.Column="8" Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="12,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" Width="36" Height="36" Margin="0,0,10,0">
                            <Ellipse Fill="#EC4899">
                                <Ellipse.Effect>
                                    <DropShadowEffect Color="#EC4899" BlurRadius="12" ShadowDepth="1" Opacity="0.6"/>
                                </Ellipse.Effect>
                            </Ellipse>
                            <TextBlock Text="🚀" FontSize="15" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Max Speed" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                            <TextBlock Text="{Binding MaxSpeedLimit, FallbackValue='Unlimited'}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                        </StackPanel>
                    </Grid>
                </Border>

            </Grid>
        </Border>

    </Grid>

</UserControl>
